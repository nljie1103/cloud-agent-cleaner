# 验证状态

核对日期：2026-08-06

本文件区分三类证据，避免把“代码测试通过”描述成“所有云平台实机卸载通过”。

## 已完成

- Python 3.8 语法兼容性解析；
- Python 编译检查；
- 16 项非破坏性单元与 CLI 回归测试；
- Agent 注册表与动作实现一一对应；
- 核心 Guest Agent 默认保护；
- 递归删除目录白名单；
- 禁止 `shell=True`、`os.system`、`curl | bash` 和明文 HTTP；
- 阿里云 Go 云监控 `stop`、`uninstall` 调用顺序测试；
- 腾讯 TAT 官方 GitHub 卸载地址回归测试；
- Azure Monitor 扩展被标记为控制面未完成，而非本地假卸载；
- GCP 2025+ manager、compat manager、core plugin 检测覆盖。

## 已对照官方文档，但尚未全部实机验证

| 平台 | 文档核对 | 真实云主机破坏性卸载测试 |
|---|---:|---:|
| 阿里云 | 是 | 部分/待补齐 |
| 腾讯云 | 是 | 待补齐 |
| AWS | 是 | 待补齐 |
| Oracle Cloud | 是 | 待补齐 |
| Azure | 是 | 待补齐；部分操作必须在控制面执行 |
| Google Cloud | 是 | 待补齐 |

## 发布原则

在每一种主要发行版和 Agent 安装形态完成可复现的创建、审计、卸载、重启、残留检查和恢复测试前，版本保持 alpha。贡献实机结果时，应记录：云平台、镜像、发行版、Agent 版本、安装形式、执行命令、退出码、残留证据和恢复方式；不得上传密钥、实例 ID、IP、账号或其他敏感信息。
